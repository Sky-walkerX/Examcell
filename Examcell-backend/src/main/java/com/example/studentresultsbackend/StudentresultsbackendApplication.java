package com.example.studentresultsbackend;

import org.springframework.boot.CommandLineRunner;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.context.annotation.Bean;
import org.springframework.security.crypto.password.PasswordEncoder;

@SpringBootApplication
public class StudentresultsbackendApplication {
	// Temporary code (e.g., in your main application class or a test)


	public static void main(String[] args) {
		SpringApplication.run(StudentresultsbackendApplication.class, args);
	}

}
